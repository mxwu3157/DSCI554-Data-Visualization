var el = d3.select('#chart4');

el.append('div')
    .text('China 1390000')
    .attr('class', 'bar')  //class is an attribute, so we use .attr()
    .style('width', '321px');

el.append('div')
    .text('India 1240000')
    .attr('class', 'bar')  //class is an attribute, so we use .attr()
    .style('width', '319px');

el.append('div')
    .text('USA 318000')
    .attr('class', 'bar')  //class is an attribute, so we use .attr()
    .style('width', '288px');

el.append('div')
    .text('Japan 127000')
    .attr('class', 'bar')  //class is an attribute, so we use .attr()
    .style('width', '267px');

el.append('div')
    .text('Germany 82800')
    .attr('class', 'bar')  //class is an attribute, so we use .attr()
    .style('width', '257px');
    
el.append('div')
    .text('UK 62783')
    .attr('class', 'bar')  //class is an attribute, so we use .attr()
    .style('width', '251px');

el.append('div')
    .text('Canada 34838')
    .attr('class', 'bar')  //class is an attribute, so we use .attr()
    .style('width', '237px');

el.append('div')
    .text('Malaysia 29240')
    .attr('class', 'bar')  //class is an attribute, so we use .attr()
    .style('width', '233px');

el.append('div')
    .text('United Arab E. 9206')
    .attr('class', 'bar')  //class is an attribute, so we use .attr()
    .style('width', '207px');

el.append('div')
    .text('Singapore 5303')
    .attr('class', 'bar')  //class is an attribute, so we use .attr()
    .style('width', '195px');